# Electron HTTP服务器部署方案

## 概述

已将射箭计时系统的Electron打包方案从`file://`协议改为使用内置HTTP服务器，前端通过`http://localhost:3000`访问，后端运行在`http://localhost:8080`端口。HTTP服务器同时处理静态文件服务和API代理。

## 主要修改

### 1. 依赖变更
- 新增 `mime@3.0.0` 依赖，用于正确设置HTTP响应的Content-Type

### 2. 关键文件修改

#### electron/main.js
- **新增HTTP服务器**：在端口3000启动HTTP服务器
- **静态文件服务**：提供前端dist文件夹中的文件
- **API代理**：将`/api/`和`/ws-archery-timer`请求代理到后端8080端口
- **顺序启动**：先启动后端，再启动HTTP服务器，最后加载前端
- **完整清理**：应用退出时关闭HTTP服务器和后台进程

#### frontend/src/services/globalWebSocketService.js
- **简化WebSocket配置**：所有环境使用相对路径`/ws-archery-timer`
- **移除复杂检测**：不再需要检测Electron环境

## 部署流程

### 开发测试
1. 构建前端：`npm run frontend:build`
2. 构建后端：`npm run backend:build`
3. 运行Electron：`npm run electron`

### 生产打包
```bash
npm run dist:win
```

## 架构说明

```
┌───────────────────────────────────────┐
│          Electron应用                 │
├───────────────────────────────────────┤
│ 1. 启动Spring Boot后端 (8080端口)     │
│ 2. 启动HTTP服务器 (3000端口)          │
│ 3. 加载BrowserWindow (http://localhost:3000)│
└───────────────────────────────────────┘
                    │
                    ▼
┌───────────────────────────────────────┐
│       HTTP服务器 (3000端口)           │
│  ├───────────────────────────────────┤ │
│  │ • 静态文件服务 (HTML/CSS/JS)      │ │
│  │ • API代理 (/api/ → 8080端口)      │ │
│  │ • WebSocket代理 (/ws-... → 8080) │ │
│  └───────────────────────────────────┘ │
└───────────────────────────────────────┘
                    │
                    ▼
┌───────────────────────────────────────┐
│      Spring Boot后端 (8080端口)       │
│  ├───────────────────────────────────┤ │
│  │ • REST API                       │ │
│  │ • WebSocket服务                 │ │
│  │ • 计时器逻辑                    │ │
│  └───────────────────────────────────┘ │
└───────────────────────────────────────┘
```

## 优点

1. **可靠性更高**：HTTP协议比`file://`协议更稳定
2. **CORS问题解决**：所有资源通过同一端口访问，避免跨域问题
3. **开发一致性**：生产与开发环境的访问方式保持一致
4. **更好的错误处理**：HTTP服务器可以提供更友好的错误页面
5. **代理支持**：可以轻松添加更多代理或中间件逻辑

## 注意事项

1. **端口冲突**：确保端口3000和8080未被占用
2. **安全**：HTTP服务器仅监听`127.0.0.1`，不对外暴露
3. **性能**：HTTP服务器代理增加了少许开销，但影响很小
4. **WebSocket支持**：当前使用HTTP代理WebSocket连接，SockJS库支持此方式

## 故障排除

### HTTP服务器启动失败
- 检查端口3000是否被占用
- 确认前端dist文件夹存在
- 查看Electron日志输出

### API无法访问
- 检查后端是否成功启动
- 确认代理规则正确匹配请求
- 查看浏览器开发者工具Network标签

### WebSocket连接失败
- 确认SockJS配置使用相对路径
- 检查HTTP服务器代理是否工作
- 查看后端WebSocket日志

## 扩展建议

### 可添加功能
1. **静态文件缓存**：为静态资源添加缓存头提升性能
2. **请求日志**：记录HTTP请求便于调试
3. **健康检查端点**：添加`/health`端点监控服务状态
4. **配置化端口**：允许用户配置HTTP服务器端口
5. **HTTPS支持**：添加本地HTTPS证书支持更安全连接

### 优化方向
1. **压缩传输**：添加gzip压缩静态资源
2. **连接池**：优化后端代理连接复用
3. **监控仪表板**：添加服务运行状态监控界面
4. **自动重试**：为后端API调用添加重试机制